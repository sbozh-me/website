# GlitchTip Error Tracking - Production Environment Configuration

# ============================================
# Database Configuration
# ============================================
# Generate with: openssl rand -base64 24 | tr -d '/+=' | head -c 32
POSTGRES_PASSWORD=changeme-strong-password-32-chars

# ============================================
# GlitchTip Core
# ============================================
# Generate with: openssl rand -hex 32
SECRET_KEY=changeme-random-secret-64-chars-required-for-security

# Domain (with https://)
GLITCHTIP_DOMAIN=https://monitoring.sbozh.me

# ============================================
# Email Configuration
# ============================================
# Sender email for error alerts
DEFAULT_FROM_EMAIL=errors@sbozh.me

# SMTP configuration (optional - for email alerts)
# Console email (development - prints to logs):
#   EMAIL_URL=consolemail://
# Gmail example:
#   EMAIL_URL=smtp+tls://user:password@smtp.gmail.com:587
# AWS SES example:
#   EMAIL_URL=smtp+tls://user:password@email-smtp.us-east-1.amazonaws.com:587
EMAIL_URL=consolemail://

# ============================================
# User Registration
# ============================================
# Set to False in production to prevent open registration
# First user to sign up becomes admin
ENABLE_OPEN_USER_REGISTRATION=False

# ============================================
# SSL (Cloudflare DNS-01 Challenge)
# ============================================
# Cloudflare API token for wildcard SSL certificate
# Create at: https://dash.cloudflare.com/profile/api-tokens
# Permissions: Zone:DNS:Edit for sbozh.me
# Store in /root/.cloudflare.ini on server (chmod 600)
# CLOUDFLARE_API_TOKEN=changeme-cloudflare-api-token
