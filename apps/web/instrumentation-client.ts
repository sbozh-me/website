// Client-side Sentry initialization is handled dynamically by SentryProvider
// This allows the DSN to be configured at runtime without baking it into the build
// See: providers/SentryProvider.tsx

export {};
