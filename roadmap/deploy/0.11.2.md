# 0.11.2 - GitOps

## Goal
Simple GitOps: deploy manifest YAML controls what version runs on server.

## Deliverables
- [ ] Create deploy manifest YAML (deploy/production/manifest.yaml)
- [ ] GitHub Action to compare manifest with deployed version
- [ ] Auto-reload docker-compose when versions mismatch
- [ ] Trigger deploy after image build for tagged versions
